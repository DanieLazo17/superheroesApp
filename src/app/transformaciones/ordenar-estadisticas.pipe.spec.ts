import { OrdenarEstadisticasPipe } from './ordenar-estadisticas.pipe';

describe('OrdenarEstadisticasPipe', () => {
  it('create an instance', () => {
    const pipe = new OrdenarEstadisticasPipe();
    expect(pipe).toBeTruthy();
  });
});
