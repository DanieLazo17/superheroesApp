import { ClasificarOrientacionPipe } from './clasificar-orientacion.pipe';

describe('ClasificarOrientacionPipe', () => {
  it('create an instance', () => {
    const pipe = new ClasificarOrientacionPipe();
    expect(pipe).toBeTruthy();
  });
});
